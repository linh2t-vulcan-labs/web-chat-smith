export interface TModelCardProps {
  className?: string;
  logo: string;
  title: string;
  description: string;
  badge?: string;
  isSelected?: boolean;
  isPremium?: boolean;
  isLocked?: boolean;
}
