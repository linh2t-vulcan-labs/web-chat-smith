export { generatePricingMetadata } from "./metadata";
export {
  assignPricingLocationHref,
  getPricingCtaPaths,
  resolvePricingCtaVariant,
  type PricingCtaPaths,
  type PricingCtaVariant,
} from "./pricing-cta";

/** Where pricing CTAs send users (subscription is handled in the main app). */
// const PRICING_SUBSCRIPTION_REDIRECT_PATH = "/manage-account/general";
