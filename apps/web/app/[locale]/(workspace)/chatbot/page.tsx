import { Chatbot } from "@/components/chatbot";

export const metadata = {
  title: "Chatbot",
  description: "Chat with the AI model of your choice.",
};

const ChatbotPage = () => <Chatbot />;

export default ChatbotPage;
